#!/usr/bin/make -f

%:
	dh $@

override_dh_install:
	./install.sh -d $(CURDIR)/debian/reversal-icon-theme/usr/share/themes
	find $(CURDIR)/debian/reversal-icon-theme/usr/share/themes -name COPYING -delete
